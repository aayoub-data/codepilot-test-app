/**
 * External link constants for the import page and related UI surfaces.
 *
 * All outbound URLs are centralised here so that link targets are updated
 * in a single place and every consumer stays in sync automatically.
 */

/**
 * URL to the Linear pitch guide — explains how to write a compelling
 * feature pitch inside Linear before kicking off an import.
 */
export const PITCH_LINEAR_URL: string =
  "https://linear.app/docs/write-a-pitch";

/**
 * URL to the Linear pilot guide — walks teams through running a
 * time-boxed pilot to evaluate Linear before a full migration.
 */
export const RUN_PILOT_URL: string =
  "https://linear.app/docs/run-a-pilot";

/**
 * URL to the Linear migration guide — step-by-step instructions for
 * migrating issues, projects and history from another tool into Linear.
 */
export const MIGRATION_GUIDE_URL: string =
  "https://linear.app/docs/migrate-to-linear";

/**
 * URL to contact CodePilot support — opens the primary help / support
 * channel where users can request assistance with an import.
 */
export const CONTACT_SUPPORT_URL: string =
  "https://codepilot.dev/support";
